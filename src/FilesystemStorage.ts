
import CustomStorage from './CustomStorage'
import SetOperation from './SetOperation'
import Get from './Get'

export default class FilesystemStorage implements CustomStorage {
    set(set: SetOperation) {
    }

    get(get: Get) {
    }
}
