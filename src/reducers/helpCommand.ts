
/*
import { Query, QueryContext } from '../query'
import DocumentMount from '../snapshot/DocumentMount'

// next:
// need QueryContext
// allow commands to announce responses through the context
// can/should terminal state be a reducer? Maybe not just yet..

export default function commandsDoc(): DocumentMount {
    return {
        name: 'helpCommand',
        value: null,
        reducer(query: Query, v, cxt: QueryContext) {
            if (query.command === 'help') {
            }
        }
    }
}
*/
