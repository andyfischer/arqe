
Implementation plan sept 12:

Introduce 'scope'
Set up tool that rins rsc -p and looks at errors
Parse error output and find the locations of errors
Tool runs callback to apply a fix 
Codebase replace Snapshot -> Scope

